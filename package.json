{
  "name": "harness",
  "version": "0.0.1",
  "description": "Harness — Custom Agent Loop built on @mariozechner/pi-ai",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -C packages/agent dev",
    "start": "node packages/agent/dist/index.js",
    "prestart": "pnpm build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
